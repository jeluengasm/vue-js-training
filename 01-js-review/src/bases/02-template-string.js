const firstname = 'Han'
const lastname = 'Solo'

const fullname = `${firstname} ${lastname}` // Template string


console.log(fullname)
console.log(`Result 1 + 2 = ${1 + 2}`) // We can put operations and functions